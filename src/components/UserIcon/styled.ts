import styled from "styled-components";

export const StyledUserIcon = styled.figure`

    background-color: var(--color-grey-3);

    border-radius: 50%;

    width: 56px;
    height: 56px;

    display: flex;
    justify-content: center;
    align-items: center;

    span{
        font-size: 32px;
        font-weight: 600;

        color: var(--total-white);
    }

`
